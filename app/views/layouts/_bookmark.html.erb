    <% if @post.bookmarked_by?(current_user) %>
      <td><%= link_to "ブックマークしています", post_bookmarks_path(@post), method: :delete, class: 'btn btn-info' %></td>
    <% else %>
      <td><%= link_to "ブックマークする", post_bookmarks_path(@post), method: :post, class: 'btn btn-secondary' %></td>
    <% end %>